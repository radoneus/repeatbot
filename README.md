# 🤖 Telegram Userbot

Userbot для Telegram з можливістю паралельних розсилок та паузою.

## 📁 Структура проєкту

```
telegram-userbot/
├── main.py                        # Основний код бота
├── database.py                    # Робота з БД
├── requirements.txt               # Python залежності
├── Dockerfile                     # Образ Docker
├── docker-compose.yml             # Конфігурація Docker Compose
├── .env.example                   # Приклад змінних середовища
├── .github/workflows/deploy.yml   # Автодеплой
└── data/                          # Сесія та БД (створюється автоматично)
```

## 🚀 Запуск

Отримайте API credentials на https://my.telegram.org:
- Увійдіть з вашим номером
- API development tools → Create application
- Збережіть `api_id` та `api_hash`

```bash
cp .env.example .env
```

Відредагуйте `.env`:

```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
PHONE=+380123456789
```

```bash
docker-compose up -d --build
docker-compose logs -f
```

## 🎮 Команди

**Префікс:** `!`

| Команда | Опис |
|---|---|
| `!spam <час> <кількість> <текст>` | Запустити розсилку. Можна кілька паралельно в одному чаті |
| `!stop <id>` | Зупинити і видалити конкретну розсилку |
| `!stop` | Зупинити і видалити **всі** розсилки |
| `!pause <id>` | Призупинити конкретну (зберігається в БД) |
| `!pauseall` | Призупинити **всі** |
| `!continue <id>` | Продовжити конкретну |
| `!continueall` | Продовжити **всі** призупинені |
| `!status` | Список всіх розсилок з прогресом |
| `!setlog` | Встановити поточний чат як чат для логів |
| `!chatid` | Показати ID поточного чату |
| `!help` | Довідка |

**Формати часу:** `30с`, `5хв`, `2г`, `1д`, `1г30хв` (або англійські: `30s`, `5m`, `2h`, `1d`)

**Приклади:**
```
!spam 30с 10 Привіт як справи
!spam 1г30хв 5 Нагадування
```

## 📝 Ліцензія

Використовуйте на власний ризик і відповідальність.

## ⚠️ Дисклеймер

Цей проєкт створено виключно для використання яке не шкодить чи заважає комусь. Автор не несе відповідальності за будь-яке використання, що порушує умови використання Telegram або законодавство. Масова розсилка може призвести до блокування акаунта.