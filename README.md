# 🤖 Telegram Userbot

Userbot для Telegram з підтримкою паралельних розсилок, розкладу за днями тижня та фіксованим часом відправки.

## 📁 Структура проєкту

```
telegram-userbot/
├── main.py                        # Основний код бота
├── database.py                    # Робота з БД
├── requirements.txt               # Python залежності
├── Dockerfile                     # Образ Docker
├── docker-compose.yml             # Конфігурація Docker Compose
├── .env.example                   # Приклад змінних середовища
├── .github/workflows/deploy.yml   # Автодеплой
└── data/                          # Сесії та БД (створюється автоматично)
    └── account_N/                 # Окрема папка на кожен акаунт
```

## 🚀 Запуск

### 1. Підготовка

Отримайте API credentials на https://my.telegram.org:
- Увійдіть з вашим номером
- API development tools → Create application
- Збережіть `api_id` та `api_hash`

### 2. Налаштування

```bash
cp .env.example .env
```

Відредагуйте `.env`:

```env
ACCOUNT_1_API_ID=12345678
ACCOUNT_1_API_HASH=abcdef1234567890abcdef1234567890
ACCOUNT_1_PHONE=+380123456789

# Для другого акаунта розкоментуйте:
# ACCOUNT_2_API_ID=...
# ACCOUNT_2_API_HASH=...
# ACCOUNT_2_PHONE=...
```

### 3. Запуск

```bash
docker-compose up -d --build
docker-compose logs -f
```

При першому запуску введіть код з Telegram.

## 🎮 Команди

**Префікс:** `!`

### Основні команди

| Команда | Опис |
|---|---|
| `!spam <текст> <затримка> <кількість> [час] [дні]` | Запустити розсилку |
| `!stop <id>` | Зупинити і видалити конкретну розсилку |
| `!stop` | Зупинити і видалити **всі** розсилки |
| `!pause <id>` | Призупинити конкретну (зберігається в БД) |
| `!pauseall` | Призупинити **всі** |
| `!continue <id>` | Продовжити конкретну |
| `!continueall` | Продовжити **всі** призупинені |
| `!status` | Список всіх розсилок з прогресом |
| `!setlog` | Встановити поточний чат як чат для логів |
| `!chatid` | Показати ID поточного чату |
| `!start` | Стартова інструкція |
| `!help` | Довідка |

### Формат команди !spam

```
!spam <текст> <затримка> <кількість> [час] [дні]
```

**Параметри:**
- `<текст>` — текст повідомлення
- `<затримка>` — інтервал між повідомленнями (`30с`, `5хв`, `2г`, `1д`)
- `<кількість>` — скільки разів відправити
- `[час]` — опціонально, фіксований час відправки (`14:30`, `2:30pm`)
- `[дні]` — опціонально, дні тижня (`пн,ср,пт` або `mo,we,fr`)

**Приклади:**

```bash
# Кожні 30 секунд, 10 разів
!spam Привіт 30с 10

# Щодня о 14:30, 30 разів
!spam Щоденне нагадування 1д 30 14:30

# Кожні 12 годин, тільки у пн/ср/пт, в той же час що і команда
!spam Робочі дні 12г 20 пн,ср,пт

# Щодня о 9:00 ранку, тільки у робочі дні
!spam Доброго ранку 1д 10 09:00 пн,вт,ср,чт,пт

# О 14:30 тільки у понеділок та п'ятницю
!spam Звіт тижня 1д 5 2:30pm пн,пт
```

### Формати часу

**Затримка:**
- `с`, `s` — секунди (`30с`)
- `хв`, `м`, `m` — хвилини (`5хв`)
- `г`, `ч`, `h` — години (`2г`)
- `д`, `d` — дні (`1д`)
- Комбінації: `1г30хв`, `2д12г`

**Час доби:**
- 24-годинний: `14:30`, `09:00`
- 12-годинний: `2:30pm`, `9:00am`, `2:30пм`

**Дні тижня:**
- Українські: `пн`, `вт`, `ср`, `чт`, `пт`, `сб`, `нд`
- Англійські: `mo`, `tu`, `we`, `th`, `fr`, `sa`, `su`

## 🔄 Логіка роботи

### Без фіксованого часу та днів
Повідомлення надсилаються через рівні проміжки від моменту запуску команди.

### З днями тижня (без фіксованого часу)
Якщо поточний день не входить у список — чекає до наступного дозволеного дня о 00:00. Далі повідомлення надсилаються через задані інтервали, але тільки у дозволені дні.

### З фіксованим часом
Перше повідомлення надсилається о заданому часі (сьогодні або завтра, залежно від поточного часу). Наступні — через задані інтервали, але завжди о тому ж часі доби.

### З фіксованим часом та днями
Повідомлення надсилаються тільки у дозволені дні о заданому часі.

## 📊 Приклади використання

**Щоденне нагадування о 9 ранку:**
```
!spam Доброго ранку! 1д 365 09:00
```

**Робочі дні о 18:00:**
```
!spam Кінець робочого дня 1д 260 18:00 пн,вт,ср,чт,пт
```

**Кожні 6 годин тільки у вихідні:**
```
!spam Нагадування 6г 20 сб,нд
```

**Щопонеділка та п'ятниці о 14:30:**
```
!spam Звіт 1д 52 14:30 пн,пт
```

## 🔧 Кілька акаунтів

Бот підтримує необмежену кількість акаунтів одночасно. Додайте в `.env`:

```env
ACCOUNT_1_API_ID=...
ACCOUNT_1_API_HASH=...
ACCOUNT_1_PHONE=...

ACCOUNT_2_API_ID=...
ACCOUNT_2_API_HASH=...
ACCOUNT_2_PHONE=...
```

Кожен акаунт має окрему БД, сесію та логи.

## 📝 Ліцензія

Використовуйте на власний ризик і відповідальність.

## ⚠️ Дисклеймер

Цей проєкт створено виключно в освітніх цілях. Автор не несе відповідальності за будь-яке використання, що порушує умови використання Telegram або законодавство. Масова розсилка може призвести до блокування акаунта.