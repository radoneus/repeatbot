# 🤖 Telegram Userbot

Userbot для Telegram з можливістю повторної відправки повідомлень через команду.

## 📁 Структура проєкту

```
telegram-userbot/
├── main.py           # Основний код бота
├── requirements.txt     # Python залежності
├── Dockerfile          # Образ Docker
├── docker-compose.yml  # Конфігурація Docker Compose
├── .env.example        # Приклад змінних середовища
├── .gitignore         # Ігнорування файлів
└── data/              # Директорія для сесії (створюється автоматично)
```

## 🚀 Швидкий старт

### 1. Підготовка

Отримайте API credentials на https://my.telegram.org:
- Увійдіть з вашим номером
- API development tools → Create application
- Збережіть `api_id` та `api_hash`

### 2. Налаштування

Створіть файл `.env` (або скопіюйте `.env.example`):

```bash
cp .env.example .env
```

Відредагуйте `.env` та вставте свої дані:

```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
PHONE=+380123456789
```

### 3. Запуск через Docker Compose

```bash
# Збудувати та запустити контейнер
docker-compose up -d

# Переглянути логи
docker-compose logs -f

# При першому запуску введіть код з Telegram
docker attach telegram_userbot
```

### 4. Зупинка

```bash
# Зупинити контейнер
docker-compose down

# Зупинити і видалити дані сесії
docker-compose down -v
```

## 🎮 Команди бота

**Префікс команд:** `!`

Після запуску відкрийте Telegram і напишіть в будь-якому чаті:

- **`!spam <час> <кількість> <текст>`**
  Запускає розсилку. Перше повідомлення буде **відредаговано** з вашої команди, а решта надіслані як нові.
  - *Час:* затримка між повідомленнями (напр. `30с`, `5хв`, `1г30хв`).
  - *Кількість:* загальна кількість повідомлень.
  - *Текст:* текст повідомлення.
  - *Приклад:* `!spam 10с 5 Привіт!`

- **`!stop`**
  Зупиняє активну розсилку в поточному чаті.

- **`!status`**
  Показує список чатів, де зараз активні розсилки.

- **`!help`**
  Виводить довідку по всім командам.

- **`!setlog`**
  Встановлює **поточний** чат як місце для надсилання логів та відповідей на команди. Налаштування зберігається автоматично між перезапусками.

- **`!chatid`**
  Показує ID поточного чату (відповідь прийде в чат для логів).

## 🛠 Альтернативні методи запуску

### Через Docker без Compose

```bash
# Збудувати образ
docker build -t telegram-userbot .

# Запустити контейнер
docker run -it --name telegram_userbot \
  -e API_ID=12345678 \
  -e API_HASH=your_hash \
  -e PHONE=+380123456789 \
  -v $(pwd)/data:/app/data \
  telegram-userbot
```

### Без Docker (локально)

```bash
# Встановити залежності
pip install -r requirements.txt

# Відредагувати main.py (вставити дані замість os.getenv)
# Запустити
python main.py
```

## 📊 Керування контейнером

```bash
# Переглянути статус
docker-compose ps

# Перезапустити
docker-compose restart

# Переглянути логи останніх 100 рядків
docker-compose logs --tail=100

# Зайти в контейнер
docker exec -it telegram_userbot /bin/bash

# Оновити код
docker-compose down
docker-compose up -d --build
```

## ⚠️ Важливо

- ⚠️ Масова розсилка порушує умови Telegram
- 🔒 Файли `.env` та `data/` містять конфіденційні дані
- 📝 Використовуйте тільки на власних акаунтах або з дозволу
- 🔐 Ніколи не публікуйте `.env` в Git

## 🐛 Вирішення проблем

### Контейнер не запускається
```bash
docker-compose logs
```

### Потрібно перелогінитись
```bash
# Видалити сесію
rm -rf data/*
docker-compose restart
```

### Оновлення коду
```bash
docker-compose down
docker-compose up -d --build
```

## 📝 Ліцензія

Використовуйте на власний ризик і відповідальність.
